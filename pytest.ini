[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    smoke: Quick startup and route checks
    unit: Unit tests (no external deps)
    integration: Needs real API key
    chaos: Chaos/resilience tests
    load: Load/performance tests
timeout = 90
